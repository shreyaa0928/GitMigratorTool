services:
  - type: web
    name: git-migrator-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: DB_PATH
        value: /opt/render/project/src/migrations.db
    rootDir: backend
